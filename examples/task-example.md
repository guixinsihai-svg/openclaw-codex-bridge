# 任务单示例：task-009

## 任务目标

请检查当前项目是否包含最小开源项目文件，并补齐缺失的文档骨架。

## 背景

这个项目用于把 OpenClaw、Telegram、本地任务文件和 Codex CLI 串起来。当前阶段只做公开项目骨架，不接入真实账号、不调用真实 Bot、不修改 OpenClaw 或 n8n。

## 输入

- 项目目录：`D:\OpenSourceProjects\openclaw-codex-bridge`
- 任务编号：`task-009`
- 允许读取：项目目录内文件
- 允许写入：项目目录内文档和示例文件

## 明确禁止

- 不要写入 API Key、Token、密码。
- 不要修改 OpenClaw。
- 不要修改 n8n。
- 不要修改计划任务。
- 不要登录 GitHub。
- 不要创建 GitHub 仓库。

## 期望输出

请生成一个结果文件，说明：

- 做了哪些文件检查。
- 创建或修改了哪些文件。
- 哪些检查已经通过。
- 哪些地方还需要人工确认。
